import java.util.Scanner;

public class Programa {
    public static void main(String[] args) {
        Scanner s = new Scanner(System.in);
        System.out.print("Código ou Símbolo ? ");
        int symb;

        //int first = word.charAt(0);
        //if (word.length() > 1) {
        //    first = (first - '0') * 10 + (word.charAt(1) - '0');
        //    if (word.length() > 2)
        //        first = first * 10 + (word.charAt(2) - '0');
        //}

        System.out.println("" + '\'' + (char) symb + '\'' + "->" + symb);
    }
}